# Core API
fastapi==0.115.12
uvicorn==0.34.0
starlette==0.46.2
pydantic==2.11.4
pydantic_core==2.33.2

# NLP — lightweight (no PyTorch/torch dependency)
spacy==3.8.4
nltk==3.9.2
numpy==1.26.4
scikit-learn==1.4.2      # TF-IDF embeddings (replaces sentence-transformers)
faiss-cpu==1.9.0
pinecone-client==5.0.1   # Cloud vector DB (alternative to FAISS)

# Google Gemini (for transcription)
google-generativeai==0.8.5

# HTTP
requests==2.32.3
httpx==0.28.1

# Utilities
python-dotenv==1.1.0
python-multipart==0.0.20

# Voice Transcription (CORE REQUIREMENT)
# API-based (Gemini API with OpenAI Whisper API fallback) is used in production.
# openai-whisper is commented out to prevent compilation errors and save memory on Render.
# openai-whisper==20240930

# NOTE: sentence-transformers is intentionally excluded.
# It pulls in PyTorch (~2GB) which exceeds Render free tier 512MB RAM.
# We use scikit-learn TF-IDF for lightweight semantic similarity instead.
# Whisper uses a lightweight model (tiny/base) to stay within memory limits.
